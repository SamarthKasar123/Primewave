# ================================
# Primewave Backend Configuration
# ================================

# MongoDB Connection
# Get this from MongoDB Atlas Dashboard -> Database -> Connect -> Connect your application
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/primewave?retryWrites=true&w=majority&appName=primewave

# JWT Secret Key
# Generate a strong secret key for JWT token signing
# You can use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secure_jwt_secret_key_here_minimum_32_characters

# Frontend URL (for CORS)
# Local development
FRONTEND_URL=http://localhost:3000
# Production (Netlify)
# FRONTEND_URL=https://primewave.netlify.app
# Custom domain
# FRONTEND_URL=https://primewave.me

# Environment
# Options: development, production, test
NODE_ENV=development

# Server Port
# The port on which the backend server will run
# Render will automatically set this in production
PORT=5000

# ================================
# Optional Configuration
# ================================

# Database Name (if different from URL)
# DB_NAME=primewave

# JWT Token Expiration
# JWT_EXPIRES_IN=24h

# API Rate Limiting (requests per window)
# RATE_LIMIT_REQUESTS=100
# RATE_LIMIT_WINDOW_MS=900000

# Log Level
# LOG_LEVEL=info

# ================================
# Production Settings
# ================================

# For production deployment:
# 1. Set NODE_ENV=production
# 2. Use your production MongoDB URI
# 3. Use your production frontend URL
# 4. Generate a strong JWT secret
# 5. Let hosting platform set PORT automatically
