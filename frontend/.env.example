# ================================
# Primewave Frontend Configuration
# ================================

# Backend API URL
# Local development
REACT_APP_API_URL=http://localhost:5000/api

# Production (Render backend)
# REACT_APP_API_URL=https://primewave-backend.onrender.com/api

# ================================
# React App Configuration
# ================================

# Environment
# Options: development, production, test
NODE_ENV=development

# Development Server Port
# The port on which the React development server will run
PORT=3000

# ================================
# Optional Configuration
# ================================

# Enable source maps in production (not recommended for security)
# GENERATE_SOURCEMAP=false

# Browser to open automatically
# BROWSER=chrome

# Enable React DevTools profiler
# REACT_APP_ENABLE_PROFILER=false

# ================================
# Production Settings
# ================================

# For production deployment:
# 1. Set NODE_ENV=production
# 2. Use your production backend API URL
# 3. Ensure REACT_APP_API_URL points to your deployed backend
# 4. Remove any development-only configurations

# ================================
# Important Notes
# ================================

# - All REACT_APP_* variables are PUBLIC and visible in the browser
# - Never store sensitive information (API keys, secrets) in frontend environment variables
# - Environment variables are embedded at build time, not runtime
# - Restart development server after changing environment variables
